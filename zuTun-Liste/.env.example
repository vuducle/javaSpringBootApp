# Copy this file to .env and edit values as needed (DO NOT commit secrets).
# API base url (no trailing slash)
API_BASE_URL=http://localhost:8088
# Login path (will be appended to base url)
AUTH_LOGIN_PATH=/api/auth/login
# LocalStorage key used for storing JWT token
TOKEN_STORAGE_KEY=jwt_token
